{
  "name": "@libre/browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 browser extension that hosts the Libre Listener Wallet and injects a WebLN (window.webln) provider so any web app can tie into the wallet.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "sync:local": "node scripts/sync-local.mjs",
    "build:local": "node scripts/build.mjs && node scripts/sync-local.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@libre/listener-wallet": "workspace:*",
    "@libre/shared": "workspace:*",
    "@scure/base": "^2.0.0",
    "lightningdevkit": "^0.1.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/qrcode": "^1.5.6",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
